# Infraestrutura e DevOps

- Dockerfile e `docker-compose.yml` prontos para execução local da API.
- Pipeline CI configurado em `.github/workflows/ci.yml`.
- Observabilidade planejada em `docs/infra/observabilidade.md`.
- Configurações de ambiente documentadas em `.env.example`.
