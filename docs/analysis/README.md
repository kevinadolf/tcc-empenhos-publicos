# Módulo de Análise

- Métricas e heurísticas implementadas descritas em `relatorio.md`.
- Código principal em `src/analysis/` (metrics, detectors, pipeline).
- Testes dedicados em `tests/analysis/`.
- Resultados são serializados pelo `AnalysisReport` e expostos via API backend.
