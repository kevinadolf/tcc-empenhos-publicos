# Frontend

Estrutura inicial reservada em `src/frontend/`. Próximos passos:
1. Definir stack (ex.: React + Vite).
2. Consumir endpoints do backend via serviços dedicados.
3. Implementar visualizações de grafo e dashboards de anomalias.
4. Criar plano de testes de UI (`tests/frontend/`).
