version: '3.9'
services:
  backend:
    build: .
    command: ["serve"]
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - .:/app

  cli:
    build: .
    command: ["cli", "summary"]
    env_file:
      - .env.example
    volumes:
      - .:/app
